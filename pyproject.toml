[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "swtipy"
version = "0.1.0"
description = "Sliding Window Technical Indicators in Python"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
authors = [
  { name="Andy", email="youremail@example.com" }
]
